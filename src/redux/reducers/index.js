import musicPlaybackReducer from '../reducers/musicPlaybackReducer';

import { combineReducers } from 'redux';

const rootReducer = combineReducers({ musicPlaybackReducer });

export default rootReducer;
